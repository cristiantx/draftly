---
"draftly": patch
---

Decorate every visible document range when block widgets or distant carets split the viewport. Preserve unique, balanced tree callbacks without skipping later content after a shared ancestor.
